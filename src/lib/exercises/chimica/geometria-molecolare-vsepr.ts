/** Pregenerated in Python with RDKit and verified there: scripts/chimica/esercizi/geometria_molecolare_vsepr.py. See pool.ts. */
import pool from './pools/geometria-molecolare-vsepr.json';
import { poolGenerator, type Pool } from './pool';

export default poolGenerator(pool as unknown as Pool);
