/** Pregenerated in Python with RDKit and verified there: scripts/chimica/esercizi/mole_massa_molare.py. See pool.ts. */
import pool from './pools/mole-massa-molare.json';
import { poolGenerator, type Pool } from './pool';

export default poolGenerator(pool as unknown as Pool);
