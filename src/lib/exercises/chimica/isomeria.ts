/** Pregenerated in Python with RDKit and verified there: scripts/chimica/esercizi/isomeria.py. See pool.ts. */
import pool from './pools/isomeria.json';
import { poolGenerator, type Pool } from './pool';

export default poolGenerator(pool as unknown as Pool);
