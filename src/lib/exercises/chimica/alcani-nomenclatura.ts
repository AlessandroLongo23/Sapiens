/** Pregenerated in Python with RDKit and verified there: scripts/chimica/esercizi/alcani_nomenclatura.py. See pool.ts. */
import pool from './pools/alcani-nomenclatura.json';
import { poolGenerator, type Pool } from './pool';

export default poolGenerator(pool as unknown as Pool);
